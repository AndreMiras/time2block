import type { FC } from "hono/jsx";

const Header: FC = () => (
  <header>
    <h1>Time to Block API</h1>
    <p>Query blockchain block numbers by timestamp with ease.</p>
  </header>
);

export default Header;
